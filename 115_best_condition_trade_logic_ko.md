# Study 115 우승 조건 매매 로직 상세 설명

## 한 줄 요약

Study 115의 최종 우승 조건은 `smc5_longonly_2021plus`다.  
핵심은 `기존 high-CAGR 베이스 엔진`은 유지하고, `롱 진입만 SMC 5-box 상단 저항 스택으로 차단`한 것이다.  
즉, **숏은 기존 방식대로 계속 받되, 나쁜 롱만 많이 잘라낸 버전**이라고 보면 된다.

## 우승 조건 성적

- Variant: `smc5_longonly_2021plus`
- CAGR: `109.5105%`
- MDD: `52.3206%`
- Calmar: `2.0931`
- Final Equity: `46701.2954`
- Trades: `123`
- Long Entries: `70`
- Short Entries: `53`
- Blocked Long by SMC: `3649`
- Blocked Short by SMC: `0`

비교 기준이었던 `currentbest_114_smc5_both_2021plus` 대비:

- CAGR `+14.4674%p`
- MDD `-0.0330%p`
- Calmar `+0.2777`

## 먼저 짚고 갈 핵심

이 우승 조건은 파일명이 `soft SR filters`이긴 하지만, 실제 우승 variant는 `SR 진입 필터를 쓰지 않았다`.

- `sr_entry_mode = "none"`
- `smc_block_mode = "long_only"`

즉, 115에서 이긴 이유는 `SR 조건 추가`가 아니라, **SMC의 상단 저항 스택을 롱에만 적용한 것**이다.

## 코드 기준 설정값

`115_backtest_btcusdt_currentbest_soft_sr_filters.py`에서 우승 조건은 아래처럼 정의돼 있다.

```python
{
    "variant": "smc5_longonly_2021plus",
    "leverage": 2.0,
    "gate_side": "short",
    "liq_window": "24h",
    "gate_bars": 8,
    "body_atr_mult": 0.25,
    "sr_entry_mode": "none",
    "smc_block_mode": "long_only",
}
```

추가 고정값:

- 초기자본: `1000 USDT`
- 수수료: 편도 `0.04%`
- 진입 마진 비중: 지갑의 `98%`
- 레버리지: `2배`
- 손절: 진입가 기준 `6%`
- 숏 익절: 계좌 수익률 기준 `15%`

## 데이터와 실행 단위

- 백테스트 구간: `2021-01-01`부터 로컬 최신 캐시 종료 시점까지
- 원천 데이터:
  - `1분봉 BTCUSDT`
  - `4시간봉 BTCUSDT`
- 실제 매매 실행 프레임:
  - `15분봉`

구조는 이렇게 나뉜다.

- `4시간봉`: 큰 방향성 판단
- `1시간봉`: 24시간 유동성 고점/저점 계산
- `15분봉`: 실제 진입/청산 실행
- `1분봉`: SR 계산용 원천 데이터

## 시장 데이터 준비 방식

### 1. 4시간봉 추세 판정

큰 방향은 `4시간봉 EMA200 + hysteresis`로 판정한다.

- EMA200을 계산한다.
- 현재 4시간봉 종가가 EMA200보다 `0.5% 이상 위`면 bullish
- EMA200보다 `0.5% 이상 아래`면 bearish
- 그 사이면 직전 상태를 유지한다
- 그리고 이 상태를 한 번 더 shift해서 `확정된 4시간 추세`만 사용한다

즉, **진행 중인 4시간봉이 아니라 이미 닫힌 4시간봉 기준으로만 방향을 쓴다**.

### 2. 24시간 유동성 레벨

1시간봉 기준으로 다음 레벨을 계산한다.

- 직전 24개 1시간봉의 최고가: `liq_high_24h_prev`
- 직전 24개 1시간봉의 최저가: `liq_low_24h_prev`

이 레벨은 숏 진입 게이트를 열기 위한 `유동성 스윕 확인용`으로 쓰인다.

### 3. 15분 ATR/EMA

15분봉에서 다음을 계산한다.

- `atr20`
- `ema20`
- 캔들 몸통 크기 `body = abs(close - open)`

이 값은 스윕이 충분히 강한지, 그리고 숏 게이트 조건을 만족하는지 판단할 때 사용된다.

### 4. SR 라인 계산

SR은 1분 원천 데이터에서 계산해 15분봉에 매핑한다.

- `white_avg = avg(EMA20[1m], EMA1800[1m])`
- `red_avg = avg(EMA20[1m], EMA1800[2m])`
- `white_floor = white_avg - ATR14[1m]`
- `red_floor = red_avg - ATR14[1m]`

다만 중요한 점은, **우승 조건 `smc5_longonly_2021plus`는 이 SR 라인을 진입 필터로 사용하지 않는다**.  
SR은 계산은 하지만, best variant에서는 매매 허용/차단 조건으로 연결되지 않는다.

## SMC 5-box 필터가 무엇을 의미하는가

### 1. 내부 오더블록 생성

SMC 박스는 15분봉 내부 구조 기준으로 만든다.

- pivot size는 `5`
- 내부 고점 레벨을 종가가 위로 돌파하면 bullish 구조 돌파로 간주
- 내부 저점 레벨을 종가가 아래로 이탈하면 bearish 구조 돌파로 간주
- 그 구간에서 가장 의미 있는 캔들을 골라 오더블록 박스를 만든다

박스는 무한히 누적하지 않고, TradingView 화면처럼 `최대 5개`만 본다.

### 2. 박스 유지/삭제

- bullish 박스는 가격이 박스 하단을 깨면 삭제
- bearish 박스는 가격이 박스 상단을 넘으면 삭제

즉, 아직 무효화되지 않은 박스만 살아남는다.

### 3. 현재 가격 기준 위/아래 박스 개수

각 15분봉마다 다음을 센다.

- 현재 가격 위에 떠 있는 bearish 박스 개수
- 현재 가격 아래에 깔린 bullish 박스 개수

여기서 `위에 있는 bearish 박스가 5개`면:

- `bearish_stack_5_above = True`

이 상태는 곧, **현재 가격 위에 저항성 공급 박스가 5개나 겹쳐 있다**는 뜻이다.

## 우승 조건의 진입 논리

## 1. 기본 방향은 4시간 추세가 결정

먼저 매 순간의 목표 방향 `desired_side`를 정한다.

- 4시간 확정 추세가 bullish면 `desired_side = long`
- 4시간 확정 추세가 bearish면 `desired_side = short`

즉, 이 전략의 가장 바깥 레벨 방향성은 여전히 `4시간 추세 추종`이다.

## 2. 롱 진입 조건

롱은 생각보다 단순하다.

필수 조건:

- 현재 4시간 확정 추세가 bullish
- 현재 포지션이 없거나, 기존 숏을 정리한 직후
- SMC 롱 차단 조건에 걸리지 않아야 함

우승 조건에서는 `gate_side = short`이기 때문에:

- 롱은 유동성 스윕 게이트를 요구하지 않는다
- SR 조건도 요구하지 않는다

즉 실제 롱 진입은 아래처럼 이해하면 된다.

1. 4시간 추세가 bullish로 확정됨
2. 현재 숏이면 우선 청산
3. 현재 15분봉에서 `bearish_stack_5_above`가 `False`여야 함
4. 그때 롱 진입

반대로 아래 상황이면 롱 진입 금지다.

- 현재 가격 위에 bearish SMC 박스가 5개 쌓여 있음

이게 바로 115 winner의 핵심이다.  
`롱 자체를 없앤 게 아니라, 위쪽 저항이 너무 두꺼울 때만 롱을 막는다`.

## 3. 숏 진입 조건

숏은 롱보다 훨씬 보수적이다.  
단순히 4시간 추세가 bearish라고 바로 들어가지 않는다.

숏 필수 조건:

- 현재 4시간 확정 추세가 bearish
- 최근 `24시간 고점 유동성 스윕`이 먼저 나와야 함
- 그 스윕 이후 `8개 15분봉` 안에 있어야 함
- 현재 종가가 `ema20` 아래여야 함

여기서 bearish sweep 이벤트는 다음을 모두 만족해야 한다.

- bearish 추세 상태
- 몸통 크기 `body >= atr20 * 0.25`
- 현재 고가가 `직전 24시간 고점`을 위로 찔렀음
- 현재 종가가 다시 그 `24시간 고점 아래`로 내려옴
- 음봉으로 마감함

이 이벤트가 발생하면 `short_gate_until = 현재 인덱스 + 8`이 열리고,  
그 기간 안에서는 숏 진입이 허용된다.

정리하면 숏은:

1. bearish 추세 확인
2. 위 유동성 스윕 발생
3. 8개 15분봉 안
4. 종가가 15분 ema20 아래
5. 그때만 숏 진입

## 4. SR 필터는 best에서 사용되지 않음

많이 헷갈릴 수 있는 부분이라 분리해서 적는다.

115 연구에는 SR 기반 진입 제한 실험이 들어 있었지만, 실제 우승 조건은 다음과 같다.

- `sr_entry_mode = none`

즉 아래 조건들은 우승 조건에 **들어가지 않는다**.

- 롱은 `white_avg` 위일 때만 진입
- 롱은 `red_floor` 위일 때만 진입
- 롱은 `red_avg` 위일 때만 진입
- 롱/숏 모두 SR soft alignment 사용

결론적으로, **115 winner는 SR보다 SMC 상단 5-box 차단이 더 중요했다**.

## 청산 로직

## 1. 공통 손절

롱/숏 모두 진입가 기준 `6%` 손절을 사용한다.

- 롱 손절가 = `entry * (1 - 0.06)`
- 숏 손절가 = `entry * (1 + 0.06)`

## 2. 청산 우선순위

포지션이 있을 때는 대체로 아래 순서로 체크한다.

1. 강제청산 가격 도달 여부
2. 손절가 도달 여부
3. 숏의 경우 15% 수익 도달 여부
4. 추세 반전으로 인한 시그널 청산 여부

## 3. 숏 전용 익절

이 전략은 **숏에만 별도 익절 잠금 로직**이 있다.

- 숏 포지션의 mark-to-market 수익률이 `15%` 이상이면 익절
- 익절 후 `locked_side = short`로 설정

이후에는 바로 숏을 다시 잡지 않는다.

- 4시간 추세가 계속 bearish면 `desired_side = 0` 처리
- 즉 같은 bearish 국면 안에서는 숏 재진입을 막음
- 이후 추세가 bullish로 한번 바뀌면 lock 해제

이 구조는 `숏 수익을 챙긴 뒤 같은 하락 구간에서 과도하게 재진입하는 것`을 줄이는 역할을 한다.

## 4. 추세 반전 청산

현재 들고 있는 방향과 `desired_side`가 달라지면:

- 기존 포지션을 현재 종가에 청산
- 필요하면 그 직후 새 방향으로 진입

예를 들어:

- bearish 구간에서 숏 보유 중
- 4시간 추세가 bullish로 확정 전환
- 기존 숏 청산
- 롱 차단 조건이 없으면 새 롱 진입

## 5. 마지막 바 청산

백테스트 마지막 바에 포지션이 남아 있으면:

- 마지막 종가로 강제 정산

## 자금 운용 방식

- 초기자본: `1000 USDT`
- 진입 시 지갑의 `98%`를 마진으로 사용
- 레버리지 `2배`
- 따라서 실질 노출은 대략 계좌 대비 `1.96배` 수준
- 진입 수수료와 청산 수수료 모두 반영

진입 구조는 DCA가 아니라 `한 번에 포지션을 열고`, 추세 전환/손절/익절 때 닫는 구조다.

## 왜 이 조건이 가장 좋았는가

핵심 해석은 간단하다.

### 1. 나쁜 롱을 많이 제거했다

실제 통계상 `blocked_long_smc = 3649`다.  
즉, 위쪽에 bearish 박스가 5개나 쌓여 있는 구간에서 롱 진입을 엄청 많이 막았다.

이건 보통 다음 상황과 비슷하다.

- 위에 공급 박스가 여러 겹
- 아직 상단 저항이 정리되지 않음
- 4시간 추세는 bullish일 수 있어도, 15분 구조상 바로 위가 막혀 있음

이런 구간의 롱을 잘라낸 것이 성과 개선의 핵심이다.

### 2. 좋은 숏까지 막지 않았다

114 winner는 `양쪽 모두` SMC 5-box를 적용했다.  
하지만 115 best는 `long_only`만 사용했다.

즉:

- 롱은 계속 깐깐하게 거르고
- 숏은 기존 best 엔진의 강점을 최대한 유지했다

이 차이 때문에:

- 114 current best: `CAGR 95.04%`
- 115 best: `CAGR 109.51%`

로 개선이 나왔다.

### 3. SR 조건은 도움이 덜했다

115에서는 여러 soft SR 필터도 같이 실험했지만, 최종 우승은 `SR 없음`이었다.

이 말은 곧:

- 현재 이 엔진에서 가장 큰 개선 포인트는 SR 정렬보다
- `상단 공급 스택에서 롱을 금지하는 것`

이었다는 뜻이다.

## 실제 매매 흐름을 문장으로 풀면

이 전략을 가장 쉽게 말하면 아래와 같다.

1. 4시간 추세가 bullish면 롱 쪽을 본다.
2. 다만 위에 bearish 오더블록이 5개나 겹쳐 있으면 롱은 하지 않는다.
3. 4시간 추세가 bearish면 숏 쪽을 본다.
4. 하지만 숏은 바로 들어가지 않고, 먼저 24시간 고점 유동성 스윕이 나와야 한다.
5. 스윕 직후 8개 15분봉 안에서 가격이 ema20 아래에 있으면 숏 진입한다.
6. 손절은 6%, 숏은 15% 수익에서 우선 익절한다.
7. 숏 익절 후에는 같은 하락 추세에서 바로 숏 재진입하지 않고, 추세가 한 번 바뀌기 전까지 잠근다.

## 실전 해석 포인트

이 best 조건이 말해주는 건 꽤 분명하다.

- 큰 방향 판단은 여전히 4시간 추세 엔진이 유효하다
- 숏은 유동성 스윕 확인이 있을 때 품질이 좋다
- 롱은 bullish 추세만 믿고 바로 사면 안 되고, 위쪽 공급 스택을 반드시 봐야 한다
- SR 라인 자체보다 `SMC 상단 저항 스택`이 이 엔진에는 더 강한 필터였다

## 다음 연구로 이어질 만한 포인트

이 문서를 기준으로 다음 개선 아이디어는 두 갈래다.

- `롱 차단`은 유지하되, `롱 재허용` 조건을 더 정교하게 만들기
  - 예: bearish stack 5가 3 이하로 줄었을 때만 롱 재허용
  - 예: bearish stack 5 위에서도 bullish BOS가 뜨면 예외 허용
- `SR`은 진입 강제 조건이 아니라 `롱 사이즈 조절`에 쓰기
  - 예: `red_avg` 위 롱은 100%, 아래 롱은 50%
  - 예: `red_floor` 아래에서는 롱 완전 금지

## 참고 코드 위치

- 우승 variant 정의: `115_backtest_btcusdt_currentbest_soft_sr_filters.py`
- SMC 롱/숏 차단 함수: 같은 파일의 `smc_entry_allowed()`
- 실제 매매 루프: 같은 파일의 `run_variant_115()`
- 2021+ 데이터 로드와 SR/SMC 피처 생성: `114_backtest_btcusdt_best_with_sr_smc_filters.py`
- 4시간 추세 hysteresis 로직: `47_backtest_btcusdt_scale06_adx002_case1_standalone.py`
- 포지션 오픈/청산/강제청산 계산: `76_backtest_btcusdt_scale06_adx002_regime_hold_leverage_sweep.py`

## 결론

Study 115의 우승 조건은 `SR을 강하게 붙인 전략`이 아니다.  
정확히는 **기존 best 추세 엔진에, "위쪽 공급 박스가 5개 겹치면 롱 금지"라는 SMC 필터만 선택적으로 추가한 전략**이다.

그래서 이 전략의 본질은:

- 방향은 4시간 추세가 정하고
- 숏은 유동성 스윕으로 품질을 올리고
- 롱은 SMC 상단 저항 스택으로 걸러낸다

라고 이해하면 가장 정확하다.
