# Study 117 우승 조건 매매 로직 상세 설명

## 한 줄 요약

Study 117의 우승 조건은 `lv2p5_g12_body20_tp20_lb5_none`이다.  
이 전략은 **Study 115의 핵심이었던 “롱 진입 시 상단 SMC 공급 5개 차단”은 유지하면서, 숏 쪽은 Study 84 계열의 더 강한 short gate와 더 큰 TP를 붙여 고-CAGR 쪽으로 밀어낸 버전**이다.

즉 아주 짧게 말하면:

- 롱: `4시간 bullish 추세 추종 + 위에 bearish OB 5개면 롱 금지`
- 숏: `4시간 bearish 추세`라고 바로 숏치는 게 아니라, `24시간 고점 유동성 스윕`이 나온 뒤 `12개 15분봉` 안에서만 숏 허용
- 추가적으로 숏 익절 목표를 `15%`에서 `20%`로 늘리고
- 레버리지를 `2.0x`에서 `2.5x`로 올린 조합이다

## 우승 조건 성적

- Variant: `lv2p5_g12_body20_tp20_lb5_none`
- CAGR: `145.2192%`
- MDD: `59.0448%`
- Calmar: `2.4595`
- Final Equity: `105,813.5209`
- Trades: `123`
- Long Entries: `70`
- Short Entries: `53`
- Blocked Long by SMC: `3649`
- Blocked Short by Gate: `17534`
- Liquidations: `0`

기준 전략 대비:

- vs `115 best`: CAGR `+35.7087%p`, MDD `+6.7242%p`, Calmar `+0.3664`
- vs `84 best reference`: CAGR `+34.1168%p`, MDD `-3.0809%p`, Calmar `+0.6711`

즉, **115보다 더 많이 벌었고 드로다운은 조금 커졌지만, 84 best와 비교하면 CAGR도 높고 MDD도 더 낮았다.**

## 코드 기준 핵심 설정값

우승 조건은 아래 설정 조합이다.

```python
{
    "variant": "lv2p5_g12_body20_tp20_lb5_none",
    "leverage": 2.5,
    "gate_bars": 12,
    "body_atr_mult": 0.20,
    "short_tp_return_pct": 20.0,
    "sr_entry_mode": "none",
    "long_block_threshold": 5,
    "short_block_threshold": 0,
}
```

의미를 풀면:

- 레버리지 `2.5배`
- 숏 게이트 지속 시간 `12개 15분봉`
- 숏 스윕 캔들은 몸통이 `ATR20의 0.20배 이상`
- 숏 익절은 계좌 기준 수익률 `20%`
- SR 진입 제한은 없음
- 위쪽 bearish OB가 `5개 이상` 쌓이면 롱 금지
- 아래쪽 bullish OB 때문에 숏을 막는 조건은 없음

## 데이터와 실행 단위

- 백테스트 구간: `2021-01-02` ~ `2026-03-15`
- 원천 데이터:
  - `1분봉 BTCUSDT`
  - `4시간봉 BTCUSDT`
- 실제 매매 실행 프레임:
  - `15분봉`

역할은 이렇게 나뉜다.

- `4시간봉`: 큰 방향 결정
- `1시간봉`: 직전 24시간 유동성 레벨 계산
- `15분봉`: 실제 진입/청산/게이트 판정
- `1분봉`: SR 라인 계산 원천

## 기본 구조: 115에서 무엇을 가져왔나

Study 117은 115를 완전히 버린 전략이 아니다.  
오히려 **115의 핵심 필터는 유지하고, 숏 엔진을 더 공격적으로 바꾼 전략**이라고 보는 게 정확하다.

115에서 그대로 가져온 것:

- 4시간 hysteresis 기반 추세 판단
- 롱 진입 시 `bearish SMC stack`으로 상단 저항을 거르는 방식
- 숏은 유동성 스윕 이후에만 진입시키는 구조
- 숏 TP 후 `locked_side`를 걸어 같은 bearish 국면 재진입을 막는 구조

117에서 바뀐 것:

- 레버리지 `2.0x -> 2.5x`
- 숏 gate bars `8 -> 12`
- 숏 sweep 강도 기준 `body_atr_mult 0.25 -> 0.20`
- 숏 TP `15% -> 20%`
- SR 필터는 끝까지 안 쓰는 쪽이 best

## 1. 큰 방향 결정: 4시간 추세

가장 먼저 매매 방향은 `4시간봉 EMA200 + hysteresis`로 정한다.

- EMA200을 계산한다
- 종가가 EMA200보다 충분히 위면 bullish
- 충분히 아래면 bearish
- 그 사이면 직전 상태를 유지한다
- 그리고 진행 중인 4시간봉이 아니라 **한 번 확정된 4시간 상태**만 사용한다

결과적으로 매 순간의 목표 방향 `desired_side`는:

- bullish면 `long`
- bearish면 `short`

이다.

즉, 이 전략의 최상위 프레임은 여전히 `4시간 추세 추종`이다.

## 2. 롱 진입 로직

롱은 의외로 단순하다.  
다만 “bullish니까 무조건 롱”은 아니고, 위에 공급이 너무 많이 쌓여 있으면 금지한다.

롱 진입 조건:

1. 현재 4시간 확정 추세가 `bullish`
2. 현재 포지션이 없거나, 기존 숏이 정리된 상태
3. 가격 위에 있는 bearish internal OB 개수가 `5개 미만`

이 전략에서 `long_block_threshold = 5`이므로:

- 현재 가격 위에 bearish OB가 `5개 이상`이면 롱 금지
- `4개 이하`면 롱 허용

그리고 우승 조건은 `sr_entry_mode = none`이기 때문에:

- `red_avg` 위여야 한다
- `red_floor` 위여야 한다
- `white_avg` 위여야 한다

같은 SR 기반 롱 제한은 **사용하지 않는다**.

즉 롱은 이렇게 이해하면 된다.

1. 4시간 bullish
2. 위에 공급 박스가 5개 이상이면 패스
3. 아니면 롱 진입

실제 성적 개선에서 이 필터가 굉장히 중요했다.  
`blocked_long_smc = 3649`였다는 건, 상단 저항이 너무 두꺼운 롱을 매우 많이 잘라냈다는 뜻이다.

## 3. SMC 5-box 필터의 의미

### 내부 오더블록 생성

SMC 박스는 15분봉 내부 구조 기준이다.

- pivot size는 `5`
- 내부 고점/저점이 종가로 돌파될 때 구조 변화로 본다
- 그 사이 구간에서 대표 캔들을 골라 오더블록 박스를 만든다

### 유지/삭제 방식

- bullish 박스는 가격이 하단을 깨면 삭제
- bearish 박스는 가격이 상단을 넘으면 삭제

### 현재 가격 기준 개수 세기

각 시점마다:

- 현재 가격 위에 있는 bearish 박스 개수
- 현재 가격 아래에 있는 bullish 박스 개수

를 센다.

117 winner는 이 중에서 오직 하나만 쓴다.

- `bearish_ob_above_count >= 5`면 롱 금지

숏에 대해서는:

- `short_block_threshold = 0`

이라서 아래 bullish 박스 개수로 숏을 막지 않는다.

즉 해석은 간단하다.

- 롱은 `위쪽 공급 스택`을 매우 민감하게 본다
- 숏은 `아래 수요 스택`보다 `스윕+추세`를 더 우선한다

## 4. 숏 진입 로직

숏은 117의 성과를 끌어올린 핵심 파트다.  
롱보다 훨씬 더 많은 조건을 요구한다.

숏 진입 조건:

1. 현재 4시간 확정 추세가 `bearish`
2. 먼저 `24시간 고점 유동성 스윕`이 발생해야 함
3. 그 스윕 이후 `12개 15분봉` 안에 있어야 함
4. 현재 종가가 `15분 ema20` 아래여야 함

### 숏 스윕 이벤트 정의

아래를 모두 만족해야 `short_sweep_event = True`다.

- 현재 4시간 추세가 bearish
- 직전 24시간 고점 `liq_high_24h_prev`가 존재
- 몸통 크기 `body >= atr20 * 0.20`
- 현재 고가가 그 `24시간 고점`을 위로 찌름
- 그런데 종가는 다시 그 고점 아래로 내려옴
- 게다가 음봉 마감

즉 단순한 고점 돌파가 아니라:

- `위 유동성 흡수`
- `빠른 되돌림`
- `bearish close`

를 모두 요구하는 구조다.

### 숏 gate 지속 시간

이 이벤트가 나오면:

- `short_gate_until = 현재 봉 index + 12`

가 된다.

그 뒤 12개 15분봉, 즉 약 3시간 동안만 숏 진입이 허용된다.

이게 중요하다.

- 115는 `gate_bars = 8`
- 117 best는 `gate_bars = 12`

즉 117은 좋은 숏 진입 창을 조금 더 길게 열어놨다.

## 5. SR 필터는 왜 best에서 빠졌나

117에서는 `long_above_red_avg`도 같이 실험했지만, 우승은 끝내 `none`이었다.

즉 이 전략에서 최종적으로 먹힌 건:

- SR 정렬 필터가 아니라
- `SMC 상단 5개 차단 + 강한 숏 gate`

조합이었다.

해석하면:

- SR은 나쁜 아이디어는 아니지만
- 이 엔진에서는 아직 결정적인 개선 포인트가 아니었다

특히 상위권을 보면 `long_above_red_avg` 버전도 `140%대 CAGR`까지는 갔지만, 최상위 `145.2192%`는 못 넘었다.

## 6. 청산 로직

### 공통 손절

롱/숏 모두 진입가 기준 `6% 손절`이다.

- 롱 손절가 = `entry * (1 - 0.06)`
- 숏 손절가 = `entry * (1 + 0.06)`

### 강제청산 계산

레버리지 `2.5배`이므로 강제청산 가격도 매 봉 체크한다.  
하지만 우승 조건은 실제 결과상:

- `liquidations = 0`

이었다.

### 숏 전용 익절

숏은 mark-to-market 기준 계좌 수익률이 `20%`에 도달하면 익절한다.

이전 115는 `15%`였는데, 117은 이를 `20%`로 올렸다.  
즉 더 오래 숏 수익을 끌고 가도록 바꾼 것이다.

### 숏 lock 로직

숏 TP가 발생하면:

- `locked_side = short`

가 된다.

그 다음엔:

- 4시간 추세가 계속 bearish면 같은 방향 재진입 금지
- 추세가 bullish로 한번 바뀌면 lock 해제

즉:

- 한 번 크게 먹은 숏은 같은 하락 구간에서 계속 과매매하지 않게 막는다

## 7. 방향 전환 청산

현재 들고 있는 포지션과 `desired_side`가 다르면:

- 현재 종가로 기존 포지션 정리
- 새 조건이 맞으면 반대 방향 진입

예:

- bearish 구간 숏 보유
- 4시간 추세 bullish 전환
- 기존 숏 청산
- 상단 5-box 차단이 없으면 롱 진입

## 8. 자금 운용

- 초기자본: `1000 USDT`
- 진입 시 지갑의 `98%`를 마진으로 사용
- 레버리지: `2.5배`
- 수수료: 편도 `0.04%`

즉 115보다 더 공격적인 노출을 쓴다.

이 부분이 CAGR 상승에 기여했지만, 동시에:

- MDD가 `52.32% -> 59.04%`

로 커진 이유이기도 하다.

## 왜 117 best가 크게 좋아졌나

핵심은 3가지다.

### 1. 롱 필터의 장점은 유지했다

115에서 이미 증명된:

- `상단 공급이 5개 겹치면 롱 금지`

이 필터를 그대로 유지했다.

이 덕분에 bullish 추세에서 무조건 롱으로 달려드는 실수를 많이 줄였다.

### 2. 숏 엔진을 더 세게 만들었다

117의 핵심 개선은 사실 숏 쪽이다.

- `gate_bars 8 -> 12`
- `body_atr_mult 0.25 -> 0.20`
- `short TP 15% -> 20%`

즉:

- 좋은 숏 진입창은 더 오래 열고
- 스윕 인정 기준은 조금 완화하고
- 먹은 숏은 더 오래 들고 간다

이게 CAGR을 크게 밀어올렸다.

### 3. SR 필터는 안 쓰는 편이 더 셌다

만약 우승이 `long_above_red_avg`였다면 SR이 핵심이었다고 볼 수 있는데, 실제 최고는 `none`이었다.

따라서 117의 본질은:

- `SR 개선 전략`

이 아니라

- `115 필터를 보존한 상태에서 84식 숏 모멘텀/스윕 엔진을 더 강하게 결합한 전략`

이라고 보는 게 맞다.

## 실제 매매 흐름을 문장으로 풀면

이 전략은 이렇게 이해하면 된다.

1. 4시간 추세가 bullish면 롱 방향을 본다.
2. 다만 가격 위에 bearish OB가 5개나 쌓여 있으면 롱을 하지 않는다.
3. 4시간 추세가 bearish면 숏 방향을 본다.
4. 그런데 숏은 바로 들어가지 않고, 먼저 24시간 고점을 스윕한 뒤 다시 그 아래로 밀리는 강한 음봉이 필요하다.
5. 그런 숏 스윕이 나오면 약 3시간 동안 숏 진입을 허용한다.
6. 그 안에서 가격이 15분 ema20 아래에 있으면 숏 진입한다.
7. 손절은 6%, 숏은 20% 수익에서 익절한다.
8. 숏 TP 후에는 같은 bearish 추세에서 바로 재진입하지 않고 한 번 잠근다.

## 이 전략의 성격

117 winner는 성격상 이렇게 보는 게 좋다.

- 상위 프레임은 추세 추종
- 롱은 보수적 필터링
- 숏은 이벤트 기반 공격 진입
- 수익은 숏에서 더 크게 끌고 감

즉 `롱/숏 대칭 전략`이 아니라:

- 롱은 “안 좋은 자리만 잘 피한다”
- 숏은 “좋은 자리만 기다렸다가 강하게 먹는다”

에 더 가까운 구조다.

## 다음 연구로 이어질 포인트

117 결과를 기준으로 다음 유망 포인트는 두 가지다.

### 1. 포트폴리오 sleeve로 넣어보기

117이 standalone에서 강해졌으니:

- `case1 + case2 + case3(=117 winner)` 형태로 넣었을 때
- 85/116 포트폴리오를 진짜 넘길 수 있는지

보는 게 자연스럽다.

### 2. 롱 필터는 유지하고 숏만 더 정교화

현재 우승 조합을 보면 이미 핵심은 보인다.

- 롱 차단은 `lb5`
- 숏은 `gate12 + tp20`

여기서 추가 실험은:

- 숏 lock 해제 조건
- 숏 gate 이후 재진입 허용 방식
- 롱 차단 완화 기준

같은 미세 조정이 될 가능성이 높다.

## 결론

Study 117의 우승 조건은 `115를 완전히 대체한 전략`이 아니라, **115의 강점이었던 롱 SMC 필터를 보존한 채 숏 엔진을 84 스타일로 더 공격적으로 강화한 전략**이다.

정확히 말하면:

- 방향은 4시간 추세가 정하고
- 롱은 상단 공급 스택 5개면 금지하고
- 숏은 24시간 유동성 스윕 이후 12개 봉 안에서만 받으며
- 숏 TP는 20%까지 길게 가져가고
- 레버리지는 2.5배를 사용한다

이 조합이 117에서 `CAGR 145.2192%`를 만든 핵심이다.
